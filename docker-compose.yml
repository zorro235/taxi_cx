services:
  taxi:
    build: .
    environment:
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
      ADMIN_TELEGRAM_IDS: ${ADMIN_TELEGRAM_IDS}
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL}
    ports:
      - "8080:8000"
    volumes:
      - taxi_data:/app/storage
volumes:
  taxi_data:
